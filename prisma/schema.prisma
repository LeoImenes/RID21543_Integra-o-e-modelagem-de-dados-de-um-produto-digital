generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Book {
  id          Int @id
  titulo      String
  num_paginas Int
  isbn        String @unique
  editora     String
}
